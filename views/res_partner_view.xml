<odoo>
  <record id="view_partner_form_library" model="ir.ui.view">
    <field name="name">res.partner.form.library</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_form"/>
    <field name="arch" type="xml">
      <xpath expr="//form/sheet" position="before">
        <header>
          <button name="%(library_management.action_membership_request_partner)d"
                  type="action"
                  class="btn-primary"
                  icon="fa-id-card"
                  string="Memberships"/>
                  context="{'default_member_id': active_id}"/>
          <field name="membership_ids" widget="statinfo" string="Memberships"/>
        </header>
      </xpath>
      <xpath expr="//field[@name='website']" position="after">
        <field name="card_id" readonly="1"/>
      </xpath>
    </field>
  </record>
</odoo>

